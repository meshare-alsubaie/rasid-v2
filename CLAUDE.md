# راصد v2 · تعليمات العمل

راصد يراقب صفحات التدريب التعاوني عند ١٢٧ جهة سعودية، محلّياً على جهاز صاحبه،
بلا أي تكلفة. النموذج محلّي عبر Ollama، مثبّت على المعالج (`num_gpu: 0`) حتى
تبقى بطاقة الرسوم فارغة للألعاب.

**معيار الفشل الوحيد:** إعلان يُنشر عند أي جهة من الـ١٢٧ ولا يصل جوّاله خلال
ستّ ساعات. هذا فشل مهما بدت البوّابات خضراء.

---

## كيف تردّ عليه

- **بالعربية.** الإنجليزية للأوامر وأسماء الملفّات والدوالّ فقط.
- **باختصار.** يقرأ على الجوّال.
- **بلا مجاملة.** إن كان الشغل سيّئاً فقل «سيّئ».
- **لا تقل «تمّ» بلا رمز خروج مطبوع.** لا تقل «أُصلح» ولا «يعمل» قبل أن تشغّل
  الأمر وتُظهر ناتجه.
- إن صحّحت نفسك فبسطر واحد، ثم أكمل. لا اعتذار ولا استرسال.

## قواعد الكتابة العربية

- **الشرطة الطويلة (—) ممنوعة** في أي نصّ يظهر على الشاشة. استعمل الفاصلة أو
  نقطتين. بوّابة `npm run test:arabic` تمنعها.
- **(ى) لا تقف مكان (ي) أبداً.** «في» لا «فى»، «التي» لا «التى».

---

## ممنوعات مطلقة

١. **لا تعدّل عتبة ولا نطاقاً ليمرّ اختبار.** إن فشل اختبار فالخطأ في الشيفرة
   أو في العيّنة، لا في العتبة. حدث هذا فعلاً: `test:classifier` كانت حمراء،
   والحلّ كان إصلاح العيّنة لا إضعاف الحارس.

٢. **لا تدمج سجلّين ولا تحذف جهة بلا إذنه.** الجهة الخاطئة تُصحَّح ولا تُدمج:
   الدمج يفقد جهة، والتصحيح لا يفقد شيئاً.

٣. **لا تطبع مفتاح API ولا أي جزء منه**، ولا حتى للتأكيد أنه موجود. اكتفِ بفحص
   أنه معرَّف.

٤. **لا تلمس مجلّد `rasid` (النسخة الأولى).** هو شبكة الأمان.

٥. **لا تحكم على صفحة حيّة من الذاكرة.** افتحها أو قل إنك لا تستطيع. ادّعاء في
   التدقيق («صفحة توظيف هيئة الاتصالات») تبيّن أنه خاطئ حين فُتح فعلاً.

---

## قبل أي تعديل: أوقف المراقب

المراقب يقرأ ملفّات `data/` في أوّل الدورة ويكتبها في آخرها. أي تعديل تكتبه
أثناء دورة طائرة **يُمحى بصمت**. حدث ثلاث مرّات.

```powershell
Stop-ScheduledTask -TaskName "RASID v2 watcher"
```

وبعد أن تنتهي:

```powershell
Start-ScheduledTask -TaskName "RASID v2 watcher"
```

تعديل الشيفرة وحدها (`src/`, `scripts/`) لا يحتاج هذا. تعديل `data/` يحتاجه.

---

## الأوامر

```bash
npm run status
```

خمس شاشات بالعربية تجيب «هل يعمل الآن؟». **هذا أوّل ما تشغّله وآخر ما تشغّله.**

```bash
npm run gates
```

٢١ بوّابة. يجب أن تخرج بـ٠. تأخذ نحو عشر دقائق.

```bash
npm run gates:local
```

ثلاث بوّابات تحتاج النموذج المحلّي أو توست ويندوز، فلا تعمل في CI. شغّلها على
جهازه وحده.

أوامر أخرى: `npm run collect` (جولة قراءة)، `npm run notify` (إرسال)،
`npm run verify-leads -- --org <id>` (فتح صفحات جهة والحكم عليها)،
`npm run build`، `npm run typecheck`.

---

## قاعدة الجودة في هذا المشروع

> **بوّابة لم تُثبَت ضدّ عطب مزروع ليست بوّابة.**

كل إصلاح لعطب يجب أن يصحبه اختبار يزرع العطب نفسه ويتأكّد أن البوّابة تحمرّ،
ثم يزيله ويتأكّد أنها تخضرّ. `scripts/test-gates.ts` يفعل هذا للبوّابات نفسها.

وقعت الحادثة مرّتين: بوّابة تطبع «pass» وهي لا تفحص شيئاً. آخرها قاعدة الشرطة
الطويلة، كان مُحدِّدها كسولاً فيقف عند أوّل حرفين عربيين، فمرّت عليه ثلاث
شرطات وطبع «pass» أربع مرّات.

---

## بيئة التشغيل

| ماذا | كيف |
|---|---|
| node في أداة Bash | `export PATH="/d/tools/node:$PATH"` قبل أي `npm` |
| المتصفّح لـPlaywright | `.playwright/` داخل المستودع، لا في ملفّ المستخدم |
| ملفّات `.ps1` | **ASCII خالص فقط.** PowerShell 5.1 يقرأ الملفّ غير المعلَّم بترميز النظام، فأيّ حرف عربي واحد يكسر المحلِّل ويوقف الجمع كلّه |
| stderr في PowerShell | 5.1 يغلّف stderr الأصلي في `NativeCommandError`، ومع `$ErrorActionPreference = "Stop"` يصير خطأً منهياً. `watch-run.ps1` يستعمل `Continue` حول الابن عمداً |
| الأسرار | `.env` و `.profile.local` و `.contact.local`، وكلّها مستثناة من git |

---

## خريطة الشيفرة

```
src/pipeline/     قلب المشروع
  model.ts        النموذج (qwen3:8b) والفرز الأوّلي
  classify.ts     استخراج الحقول، وحارس «منقول حرفياً»
  opportunity.ts  بناء السجلّ، وحلّ التواريخ، والرايات
  relevance.ts    درجة الصلة، حسابية بحتة بلا نموذج
  schedule.ts     الجدولة الطبقية وأرضية المضيف (١٢ دقيقة لكل مضيف)
  notify.ts       اختيار ما يُرسَل، وساعات الهدوء بتوقيت الرياض
  robots.ts       الصلاحيات، وتُحترم دائماً
  address.ts      متى يكون رابطان صفحة واحدة
src/hijri.ts      تحويل أمّ القرى بجدول، لا بنموذج، أبداً
src/types.ts      العقد، والرايات، و statusFor
src/app/          الواجهة، عربية بالكامل
scripts/          الجامع والمُشعر والبوّابات
data/             البيانات، وتُنشر على الموقع
```

## عقود لا تُكسر

- **النموذج المجّاني لا يملك أن يرفض.** أي خطأ أو مهلة أو ردّ غير مفهوم
  **يمرّر الصفحة**، ولا يُسقطها.
- **الدرجات حسابية.** النموذج لا يُسأل عن صاحب التطبيق ولا يُعطي درجة.
- **التواريخ الهجرية بجدول أمّ القرى**، ولا تُقبل من النموذج أبداً.
- **الحقل المنقول يجب أن يوجد في نصّ الصفحة.** حارس `notCopiedFrom`.
- **لا تاريخ خير من تاريخ خاطئ.** فقدان موعد يعني فصلاً ضائعاً.

---

## الحالة اليوم · 2026-09-03

**التدقيق:** ١٠٨ أعطاب سُجّلت. ٥٠ قاتلاً أُصلحت كلّها، وكذلك الثانوية
والتجميلية، عدا ثلاثة.

**الموقوف:** `ث‑٤٦` و `ث‑٤٧` و `ث‑٤٨` في `AUDIT.md` تحتاج فتح صفحات حيّة.
الأوامر التي تغلقها مكتوبة هناك. **قرار حذف جهة أو إبقائها قراره هو.**

**ما يستحقّ النظر:** طابور الحكم. لو بقي رقمه في `npm run status` ثابتاً بين
يومين، فالتصنيف متوقّف والسبب غالباً أن Ollama لا يعمل.

**ملفّات عمل، ليست وثائق:** `AUDIT.md` و `استأنف-من-هنا.md` مستثناة من git
عمداً. المستودع عامّ وسيعطيه لأصدقائه.
