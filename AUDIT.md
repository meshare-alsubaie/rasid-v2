# التدقيق النهائي

سجلّ الأعطاب. مُلئ أولاً، ولم يُصلَح منه شيء حتى اكتمل.

بدأ: 2026-09-02 · عشرة مدقّقين على عشر جبهات، وقياس ميداني واحد.
**اكتمل الكشف: 2026-09-02.** ستّة مدقّقين أكملوا، وأربعة سقطوا بحدّ الجلسة
(الجدولة والإشعارات والخصوصية وسلامة البيانات) وغُطّيت جبهاتهم يدوياً،
ثم مراجعة ثانية على الزوايا التي لم يُكلَّف بها أحد.

الحصيلة: **٥١ عطباً قاتلاً · ٤٨ ثانوياً · ٩ تجميلية.** المجموع ١٠٨.
الإصلاح: لم يبدأ. يبدأ بالترتيب المكتوب في `استأنف-من-هنا.md`.

---

## القياس الميداني: ما يقرؤه هذا الجهاز فعلاً، اليوم

فُتح كل مصدر موثّق (٤٢٢) من جهازك بآداب الزاحف نفسها، وقُورن الواقع بسجلّ الصحّة.

| النتيجة | العدد |
|---|---|
| قُرئ فعلاً | ٣٢٥ |
| نصّ ضئيل (أقل من ٤٠٠ حرف) | ٤٧ |
| فارغ تماماً | ٢١ |
| تعذّر الوصول | ١٩ |
| صفحة مفقودة تردّ ٢٠٠ | ٨ |
| يمنعها robots | ٢ |

**سُجِّل «سليماً» وهو غير مقروء الآن: ٦٧ مصدراً.**
**سُجِّل «معطوباً» وهو يُقرأ الآن: ٢**

### والرقم الأخطر في التدقيق كلّه

**التطبيق يقول «٤ جهات بلا مصدر مقروء». الحقيقة ٢٩.**

منها بالطبقة S: `pif` (٤٠٣)، `site` (صفحة فارغة تماماً).
وبالطبقة A: `albilad` (رابطان ميتان يردّان «Error»)، `elm`، `mobily`
(صفحة تدريبها التعاوني المؤكّدة تعطي صفر حرف)، `moj`، `simah`، `thiqah`، وغيرها.

هذا ضوء أخضر كاذب على ٢٥ جهة، وهو بالضبط ما تخافه.

---

## القاتلة

### ق‑١ · جولة تُقتل بعد ١٢ دقيقة، والجدول يتقدّم كأنها تمّت
`watch.ts:176` يقتل الابن بعد ٧٢٠ ثانية، و`collect.ts:640` ميزانيته ٩٠٠ ثانية
**للاستدلال وحده**. والملفّات لا تُكتب إلا في `collect.ts:918`، بعد كل شيء.
فالقتل يُلقي نتائج الجولة كاملة، ثم `watch.ts:241` يعلّم الأربعين «فُحصت».
**مؤكَّد: الرقمان متضاربان بالبناء، والكتابة في السطر ٩١٨.**

### ق‑٢ · `--limit` بلا رقم يمسح البيانات كلّها
`Number(undefined)` = `NaN` ← `wholeRun` يصير **صحيحاً** و`slice(0, NaN)` يعيد **فارغاً**.
**شغّلته وأثبتّه.** خطأ مطبعي واحد يمسح ٤٢٢ بصمة وكل `firstSeenISO`.

### ق‑٣ · فشل التصنيف مرّتين متتاليتين **يحذف حكماً حقيقياً**
`collect.ts:775` يبحث عن سجل `!flags.includes("needs_manual_review")`، وهو الوسم
الذي أضافته الجولة السابقة. فلا يجده فيحذف كل سجلّات المصدر ويضع بطاقة فارغة.
**التعليق فوقه يقول عكس ما يفعله الكود.** والفشل المتتالي هو الحالة العادية.

### ق‑٤ · بصمات الكتل تُخزَّن بلا حكم، فالإعلان يصير «قديماً» ولا يُقرأ أبداً
جولة `--no-classify` (وأنت تلعب) تخزّن بصمة كتلة الإعلان بلا حكم. الجولة التالية
تستثنيها لأنها «معروفة»، فيُرسَل تغيّر شريط الأخبار بدل الإعلان، ثم يُمسح الدَّين.

### ق‑٥ · «لا» من سؤال الكلمة الواحدة تُخزَّن حكماً نهائياً
`collect.ts:735` يكتب `verdicts.set(fingerprint, null)`. خطأ واحد من النموذج
المحلّي يصير حكماً دائماً على صفحة لا تتغيّر. **مؤكَّد.**

### ق‑٦ · روابط التدريب فوق حدّ العشرين تُختم «معروفة» ولا تُقترح أبداً
`watch-sitemaps.ts:212` يقترح ٢٠ ويخزّن **كل** البصمات.

### ق‑٧ · استثناء في إغلاق المتصفّح يُنهي العملية قبل أي كتابة
`browser.ts:135` بلا حماية، والاستدعاء على المستوى الأعلى.

### ق‑٨ · كروميوم غير مثبَّت: ١٧ مصدراً ميت بأمر واحد
منها **صفحات سدايا الثلاث للتدريب التعاوني**، وصفحة موبايلي المؤكّدة،
و`moj` و`albilad` و`maaden`. أغلى فجوة تغطية في المشروع، وأرخص إصلاح.

### ق‑٩ · لافتة «موقع حكومي» تُستخرَج بدل الصفحة
**٣٠ سجلاً من ١٤٥** نصّها الخام هو لافتة تسجيل gov.sa، **وستّة منها صُنِّفت إعلانات
حقيقية**، منها سدايا (٧٠) و`ncac` (٩٠). أي أن الحكم بُني على نصّ ليس من الصفحة.

### ق‑١٠ · سجل مُختلَق بدرجة ٩٠ في أهمّ جهة سيبرانية
`ncac` عنوانه «البراغات الالميدية» وسببه «الديراسة الانية الصيبراني»، ليست كلمات.
ومصدره لافتة gov.sa. وهو الأعلى ترتيباً في السيبراني كلّه.
وموقع الأكاديمية الحقيقي `ncac.edu.sa` **غير مجلوب أصلاً**.

### ق‑١١ · عامل الخدمة ينهار عند **كل** إشعار
`sw.js:113` يستعمل `entry` خارج نطاقه ← `ReferenceError` دائم.
فـ`lastServerPush` لا يُكتب أبداً، فالتطبيق يعرض سطرين متناقضين، **وإنذار
«لم يصلك إشعار منذ مدّة» لا يمكن أن ينطلق أبداً**. حارس القناة الميّتة ميّت.

### ق‑١٢ · ٣٨ مصدراً معلَناً لا يُجلب أبداً، ٢١ منها صفحات تقديم
منها صفحة `dga` للتدريب التعاوني حرفياً، وإعلان موبايلي، و`ncac.edu.sa`.
ولا شيء في الواجهة يقول إنها موجودة وغير مقروءة.

### ق‑١٣ · مسار جهازك يظهر لأصدقائك
`main.ts:1008` يطبع `lastError` خاماً: **٥٦ سطراً إنجليزياً، ١٨ منها تحوي**
`C:\Users\GIGABITE\AppData\Local\ms-playwright\...`

### ق‑١٤ · معرّفات لاتينية مكرّرة في لافتة الصدارة
`main.ts:994` يطبع `sdaia، albilad، alinma، maaden، maaden، ... pwc_sa ×٧`
والاسم العربي متاح على بُعد سطرين.

### ق‑١٥ · شاشة الاشتراك نموذج تسليم لمطوّر
`main.ts:1273` يعطي الصديق مربّع JSON خاماً واسم متغيّر بيئة. **ولا يوجد أي مسار
يجعل اشتراك صديقك يصل إلى المُرسِل**، الإشعارات لا تعمل لأحد غيرك.

### ق‑١٦ · ٧٤ بطاقة تتحدّث عن فاتورة، تحت لافتة حمراء دائمة
`main.ts:382` لافتة «لم يُحكَم على ٩٦» مشتعلة دائماً، وتحتها بطاقات تقول
«أيام كان التصنيف **مدفوعاً**... بلا **كلفة**».

### ق‑١٧ · التطبيق يخاطب شخصاً واحداً
«تخصصي مقبول» ×١١٥، و«وأنت غير مؤهّل قبل التخرّج» تقرير عن حالة القارئ،
ودرجة «صلة» محسوبة لشخص آخر معروضة بأكبر خطّ في البطاقة.

### ق‑١٨ · مصفاة القطاع إنجليزية بالكامل
`main.ts:595`: `bank, consulting, finance, gov, health, industrial, semi_gov, startup, tech`

### ق‑١٩ · `validate` يمرّ على مجموعة بيانات فارغة
لا تأكيد في أي مكان أن أي ملفّ يحوي سجلاً واحداً. `[]` يمرّ ويُنشر.

### ق‑٢٠ · الخطأ رقم ٥ بلا بوابة، وثلاث بوابات **تدّعي** تغطيته
`test-faults.ts:137` يمرّر `true` حرفياً. و`test-lifecycle.ts:140`
و`test-longrun.ts:86` يؤكّدان قيماً وضعاها بأنفسهما.
الكود الحقيقي `collect.ts:842` **لا يشغّله أي اختبار**.

### ق‑٢١ · الخطأ رقم ٧ بلا بوابة
`readsAsNo` يحمل الإصلاح ولا يستورده اختبار واحد. لو رُدّ الخطأ لعادت
كل «لا» عربية تُقرأ «نعم»، ولا بوابة تلاحظ.

---

## الثانوية

- **ث‑١** `verify-leads` يستبدل صفحة عميقة فشلت مؤقّتاً بجذر الموقع، نهائياً.
- **ث‑٢** حذف مكرّرات يزيل الشقّ بعد `#`، فيدمج صفحتين مختلفتين في تطبيق أحادي.
- **ث‑٣** `robots.ts:65` يقبل ردّ HTML بحالة ٢٠٠ ملفَّ robots مسموحاً.
- **ث‑٤** إعادة التوجيه داخل المضيف نفسه لا يُعاد فحصها.
- **ث‑٥** `organisations.json` (٥١٠ ك.ب) يُكتب بلا ذرّية في ثلاثة سكربتات.
- **ث‑٦** فشل قراءة خريطة الموقع يُسجَّل «هذا الموقع لا يعلن خريطة».
- **ث‑٧** `test-classifier` خارج CI بسبب **مذكور خطأً**؛ السبب الحقيقي ملفّ
  الطالب المستثنى، وحلّه سطر واحد مستعمَل في ملفّ مجاور.
- **ث‑٨** `test:gates` يزرع عطباً في ٣ بوابات من ٢٣، ويطبع «كل بوابة تفشل حين يجب».
- **ث‑٩** `benchmark-cases` يؤكّد نطاق ٥٥–٨٩ لنطاق تنصّ المواصفة أنه ٦٠–٨٥.
- **ث‑١٠** `audit-privacy` يستثني `opportunities.json` من قاعدة المعدّل التراكمي،
  وهو الملفّ الذي يحمل `relevanceReason`.
- **ث‑١١** `audit:contrast` يقيس ولا يؤكّد أنه قاس شيئاً، ويثبّت خلفية الصفحة فاتحة.
- **ث‑١٢** `audit-domains` ليس بوابة: لا `exit(1)` فيه ولا وجود له في CI.
- **ث‑١٣** ٤١ جهة مراقَبة على جذر موقع أو مركز إعلامي فقط.
- **ث‑١٤** لا يوجد `npm test` جامع؛ «شغّل كل البوابات» يعني ١٧ أمراً.
- **ث‑١٥** إمكانية الوصول: بؤرة التركيز تُدمَّر عند كل نقرة، ولوحة SVG بمئة
  محطّة تنقّل بلا اسم، وثلاثة تبويبات تقفز من h1 إلى h3.
- **ث‑١٦** مصافٍ لا تُطابق شيئاً أبداً: «لها مكافأة معلنة» (٠ من ١١٥)، والمدن (٥ من ١١٥).
- **ث‑١٧** ٢٢ شرطة طويلة في نصوص تُعرض، منها اسم التطبيق على الشاشة الرئيسية.

---

## التجميلية

- خلط الأرقام العربية والغربية في الجملة الواحدة.
- «١ نافذة مفتوحة» بدل «نافذة واحدة».
- زرّ الطابع الافتراضي بلا ألوان معاينة (`observatory` غير معرَّف في `style.css`).
- `theme-color` ثابت مع ستّة طباعات.

---

## ما أضافه تدقيق الإشعارات والخصوصية (٢٠٢٦‑٠٩‑٠٢)

### ق‑٢٢ · قناة الجوّال ميتة منذ ٤٧ ساعة، وكل جولة تنتهي «ناجحة»
**تحقّقتُ منه بنفسي:** آخر تسليم فعلي `2026-08-31T06:25Z`. في السجل **٢٨٢** سطر
`push failed for …: Received unexpected response code`، ومعها **١٣٩** سطر
`notify exit 0`. و**٥١** إشعاراً معلّقاً، منها ١٥ فرصة.
`scripts/notify.ts:193` يلتقط الفشل ويطبعه في ملفّ مستثنى من git، ثم يخرج بصفر.
لا إنذار، ولا خروج غير صفري، ولا شيء يميّز «جولة سليمة» من «انقطاع تامّ».
وتُهمَل `WebPushError.statusCode`، فلا يُعرف أهو ٤١٠ (اشتراك ميت) أم ٤٠٠ (مفتاح خاطئ).

### ق‑٢٣ · المراقب لا يدفع إلى GitHub إطلاقاً، والموقع متجمّد
`git push` موجود في `scheduled-run.ps1:150` وحده، سكربت **راصد ١ المُطفأ**.
`watch.ts` و`watch-run.ps1` بلا أي عملية git.
**تحقّقتُ:** محلياً `2026-09-02T06:00Z`، وحيّاً `2026-09-02T00:36Z`، وخمسة ملفّات غير مدفوعة.
إطفاء راصد ١ قطع خطّ النشر ولم ينتبه أحد.

### ق‑٢٤ · اسم حساب ويندوز منشور على الموقع العام
**تحقّقتُ:** `curl …/data/health.json | grep -c GIGABITE` ← **١٥**.
ومثلها في `organisations.json` (٦) و`verification.json` (٦).
و`audit-privacy.ts` **لا يملك قاعدة للمسارات المحلّية أصلاً**.

### ق‑٢٥ · `announce()` يجعل سجلّ الإثبات يلغي الإشعار
`sw.js:97` ينتظر `recordArrival` **قبل** `showNotification`. فشل الذاكرة المؤقّتة
(ضغط تخزين، تصفّح خاص) يُسقط الإشعار بلا أي أثر. الترتيب معكوس: الإنذار أولاً.

### ق‑٢٦ · أربعة أنواع إشعار تُقرَّر مرّة وتُحذف بعد ٧ أيام بلا أثر
`opened` و`few_seats` و`source_broken` و`closing_soon` تنطلق على **انتقال حالة** فقط.
الطابور نسختها الوحيدة، ويُقلَّم بلا شرط (`notify.ts:109`). بعد الحذف لا يمكن
إعادة اشتقاقها. و`opened` وزنها ٤٠٠+، أي «نافذة فُتحت»، وهو سبب وجود المشروع.

### ق‑٢٧ · أربع فرص حقيقية عالقة بأوزان ما قبل الإصلاح، وتتبخّر ٢٠٢٦‑٠٩‑٠٧
الوزن **يُحفَظ** في `pending-notices.json`، و`carried` يسبق `fresh` في إزالة التكرار،
فالوزن القديم يغلب دائماً. أربعة إشعارات بأوزان ٧٠/٧٠/٦٥/٦٥، تحت كل شكوى «مصدر معطوب» (١٥٠).
**ولا يمكن علاجها:** معرّفاتها لم تعد في `opportunities.json`.

### ق‑٢٨ · تشغيل البريد سيحوّل ٤٤ إشعاراً غير مُسلَّم إلى «مُسلَّم» نهائياً
`sendDigest` يعيد **قيمة منطقية واحدة للدفعة كلّها**، والمتصل يعلّم كل مفاتيحها مُرسَلة.
`sendPush` يعيد المفاتيح الناجحة وحدها، وتعليقه يشرح لماذا العدّ خطأ، ثم يفعله البريد.

### ق‑٢٩ · تذكير الأسبوعين يُعلَّم «أُرسل» بلا إرسال
`main.ts:802`، `reg.active?.postMessage(...)` ثم `reminded[id] = now` بلا شرط.
إن كان العامل غير مفعّل (الحالة الطبيعية بعد أي نشر) لا يُرسل شيء ويُلغى التذكير للأبد.

### ق‑٣٠ · ميزانية الإشعارات لا تكفي أصلاً
الحدّ ٦ يومياً، والوارد المقيس ≈٨٤ يومياً. `source_broken` يأخذ **صفر** خانة.
التوازن: ≈٥٩٠ إشعاراً معلّقاً دائماً، كلٌّ يموت عند اليوم السابع بلا تسليم.
**وأسوأ زمن من فتح النافذة إلى رنّة الجوّال ≈٧٨ ساعة**، والوعد ٦.

### ق‑٣١ · الملفّ الشخصي يتسرّب عبر `relevanceReason` إلى الموقع الحيّ
في الملفّ المنشور: «محاضراته المسائية» ×١، «تخصص الطالب» ×٦، «لم يتخرج» ×٣.
و`audit-privacy.ts:50` **يستثني `opportunities.json`** من قاعدة المعدّل التراكمي.

### ق‑٣٢ · جامعته وتخصّصه في الشجرة العامة
`test-relevance.ts:40` و`ابدأ-من-هنا.md:11`، وفي التاريخ عند `4085258`.

### ث‑١٨ · `audit:privacy` لا يستطيع منع النشر
يعمل في `validate.yml`، والنشر في `collect.yml`، مساران متوازيان بلا `needs:`.

### ث‑١٩ · التوست يكرّر ٦ إشعارات كل ٦٠ ثانية
خارج سجلّ الإرسال عمداً وهو صحيح، لكن مع ق‑٢٢ صار ٢٨٢ توستاً في ساعتين.
الحلّ: ذاكرة توست منفصلة عن سجلّ التسليم، لا انعدام ذاكرة.

### ث‑٢٠ · الحدّ اليومي بتوقيت UTC وساعات الهدوء بتوقيت الرياض
`dayOf` شريحة UTC، فالميزانية تُصفَّر ٣ فجراً بتوقيتك، داخل نافذة الهدوء.

### ث‑٢١ · لا حدّ إطلاقاً أثناء ساعات الهدوء
الفرع الهادئ يعود قبل حساب `room`، فعشرون `closing_soon` تُدفع كلّها وتستهلك ميزانية الغد.

### ث‑٢٢ · README يرسل أصدقاءك إلى الموقع القديم
`README.md:6` و`:285` يشيران إلى `/rasid/` لا `/rasid-v2/`، ويقولان «يسأل Claude كل ٦ ساعات».

### ث‑٢٣ · لا رخصة، ومسار `D:\tools\node` مثبّت في ثلاثة ملفّات
### ث‑٢٤ · `watch.log` بلا تدوير (١ ميجابايت)، وهو السجلّ الوحيد لكل ما سبق
### ث‑٢٥ · `AUDIT.md` غير متتبَّع وغير مستثنى، وفيه مسار محلّي

---

## ما أضافه تدقيق الجدولة والمراقب (٢٠٢٦‑٠٩‑٠٢)

### ق‑٣٣ · سطر واحد على stderr يقتل المراقب كلّه، والادّعاء المركزي للتصميم باطل
`watch-run.ps1:9` يضع `$ErrorActionPreference = "Stop"`، و`watch-run.ps1:80` يدمج
stderr الابن بـ`2>&1`. في PowerShell 5.1 هذا الدمج يغلّف كل سطر خطأ في
`NativeCommandError`، ومع `Stop` يصير **خطأً منهياً**. أول بايت يكتبه node أو أي
حفيد له على stderr يُنهي الأنبوب.

و`runNode` يستعمل `stdio: "inherit"`، فـstderr الجامع والمُشعر يذهب إلى الأنبوب نفسه.

**التصميم يقول مرّتين إن انهيار الدورة لا يأخذ المراقب** (`watch.ts:19` و
`watch-run.ps1:76`). **الادّعاء باطل: stderr الابن مفتاح قتل للأب.**

**وقد وقع فعلاً في السكربت الشقيق:**
`scheduled-run.log:35` → `error: node:internal/modules/run_main:107`
وهو **أول سطر من أثر مكدّس**، ابتلعه الـcatch، وضاع باقي الأثر فلا يُعرف الخطأ أبداً.

### ق‑٣٤ · ملفّ جدولة يُحلَّل لكنه ليس مصفوفة يُجمّد المراقب حيّاً إلى الأبد
`watch.ts:117` يلتقط `JSON.parse` وحده. ملفّ فيه `null` أو `{}` يُحلَّل بنجاح
ثم ينفجر داخل `reconcile`. و`state` يُسنَد **مرّة واحدة** في السطر ٢٥٩، فالقيمة
المسمومة تُعاد كل ٦٠ ثانية للأبد. العملية حيّة، وتطبع سطراً كل دقيقة، ولا تجمع شيئاً.
و`saveState` كتابة غير ذرّية، فانقطاع كهرباء أثناءها يكفي.

### ق‑٣٥ · لا شيء في المشروع كلّه يسحب `nextCheckAt` إلى الوراء
`clampAfterSleep` يؤخّر فقط، ولا يمسّ إلا المتأخّر أصلاً. و`reconcile:276` يحافظ
على تاريخ مستقبلي قديم حرفياً. **جُرّب ٢٠٠ دورة على مدخل تاريخه بعد ٤٠٠ يوم: لم يتغيّر.**
ساعة نظام متقدّمة لدقيقة واحدة (بطارية BIOS، إقلاع مزدوج) تجعل كل مصدر تلمسه
تلك الدورة **غير مقروء لسنة كاملة**، والمراقب يبدو سليماً تماماً.

### ق‑٣٦ · `tasklist.exe` بلا مهلة: تجميد كامل بلا سطر واحد
`watch.ts:108`، `spawnSync` بلا `timeout`، بخلاف `runNode` الذي يمرّرها.
تعليق `tasklist` يوقف الخيط: العملية حيّة، المهمّة «تعمل»، صفر سطور، صفر دورات، للأبد.
وهو **العطب الوحيد الذي لا يُنتج أي مخرَج إطلاقاً**.

### ق‑٣٧ · المراقب لا يكتب نبضة، والحارس الوحيد معاير على إيقاع ألغيناه
`watch.ts:15` يدّعي «النبضة تُكتب كل دورة». **لا شيء في الملفّ يكتب نبضة.**
ساعة التطبيق تُشتقّ من `health.json` وحده، أي من جولة **اكتملت**.
و`main.ts:341` ينذر بعد **١٢ ساعة** ونصّه يقول «والمفترض كل ٦».
المراقب يدور كل دقيقة الآن. **يمكن أن يموت ١١ ساعة و٥٩ دقيقة والتطبيق لا يقول شيئاً**
أي نحو ٧٠٠ دورة ضائعة داخل النقطة العمياء للحارس.
**وتأكيد حيّ:** الجولة التي قُتلت اليوم `06:24 · collect exit 124` لم تكتب شيئاً،
و`watch.ts:241` علّم ٣٤ مصدراً «فُحصت» ولن تُقرأ قبل ١٢:٢٤.

### ث‑٢٦ · لا حارس نسخة واحدة، والمثبّت نفسه يدعوك لتشغيل مراقب ثانٍ
لا قفل ولا mutex في المشروع كلّه. و`install-watch.ps1:123` يطبع أمر التشغيل اليدوي.
مراقبان يتقاسمان `schedule.json` بآخر-كاتب-يفوز و`due.txt` بمسار واحد ثابت،
فـ`markChecked` لأحدهما يُمحى، وتصير المصادر مستحقّة فوراً **متجاوزة أرضية المضيف كلّها**.

### ث‑٢٧ · `scheduled-run.ps1` يتجاوز أرضية المضيف تماماً
`npm run collect` بلا `--urls`: مسح كامل لـ٤٢٢ مصدراً، يتجاهل الجدول والطبقات
وأرضية الاثنتي عشرة دقيقة، ولا يحدّث الجدول بعده. `alinma.com` يأخذ ٢٤ طلباً دفعة.
(المهمّة مطفأة الآن، لكن `install-schedule.ps1` هو الطريق الموثّق لإعادتها.)

### ث‑٢٨ · مسار مؤقّت مشترك في `writeAtomic` يكسر الذرّية بين عمليتين
`${path}.tmp` اسم ثابت. جامعان متزامنان: أحدهما ينشر محتوى الآخر، والثاني يرمي
`ENOENT` على المستوى الأعلى، فيموت بعد كتابة `health.json` وقبل `snapshots.json`.

### ث‑٢٩ · قتل الابن لا يقتل أحفاده
`spawnSync` على ويندوز ينهي العقدة المباشرة فقط. و`browser.ts` يحمل متصفّحاً
دائماً على مستوى الوحدة، و`closeBrowser` يُتخطّى عند القتل. كل دورة تنتهي بمهلة
قد تترك شجرة كروميوم على جهاز صاحبه يلعب عليه.

### ث‑٣٠ · `install-schedule.ps1` يُلغي `fix-schedule.ps1` بصمت
يعيد `-Force` مع `ExecutionTimeLimit 30m` وبلا إعدادات البطارية، وهي الثلاثة
التي وُجد `fix-schedule.ps1` لإصلاحها.
**وتأكيد حيّ:** `scheduled-run.log` يظهر جولة ١ سبتمبر ماتت بعد خرائط المواقع
وقبل الجمع، `LastTaskResult 267014` = قتلها المجدول، **ولم تكتب كلمة واحدة**.

### ث‑٣١ · مسح الاكتشاف يُتخطّى بلا أثر كلّما تأخّرت جولة منتصف الليل
`scheduled-run.ps1:129` مشروط بـ`$hour < 6`، و`-StartWhenAvailable` يجعل الجولة
الفائتة تعمل متأخّرة. جهاز مطفأ ليلاً ← **الاكتشاف لا يعمل أبداً**، وهو الدفاع
الوحيد عن إعلان يُنشر على عنوان جديد.

### ث‑٣٢ · «nothing due; next in 0 min» يعني عكس ما يبدو، وطُبع ٢٢٥ مرّة
**قِيس:** ٢٢٥ سطراً بـ«0 min»، و**صفر** سطر بأي رقم آخر.
المعنى الحقيقي: مصادر **متأخّرة** ومحجوبة بأرضية المضيف. و`Math.max(0, …)`
يمحو الإشارة الوحيدة التي تفرّق بين «منتهٍ» و«مخنوق بطابور».

### ث‑٣٣ · ملفّ جدولة مفقود يمحو ذاكرة أرضية المضيف
`dueNow` يتخطّى `lastCheckedAt === null`، فبعد انهيار وإعادة بناء يصير
`lastByHost` فارغاً، ومضيف قُرئ قبل ٦٠ ثانية يصير مؤهّلاً فوراً.

### ث‑٣٤ · `env.ts` يفقد أول متغيّر أمام BOM، ولا يقول ماذا حمّل
`collect.ts:91` ينزع BOM لأن «محرّرات ويندوز تضيفه بصمت»، و`env.ts` لا ينزعه.
و`loadEnvFile` يعيد قائمة المفاتيح ويرميها المتصلان بلا تسجيل.

### ث‑٣٥ · الأمر الذي يعطيه المثبّت لقراءة السجلّ يُفسده
`install-watch.ps1:126` يقترح `Get-Content` بلا `-Encoding UTF8`، فيعرض العربية
حروفاً مشوّهة. السجلّ نفسه سليم.

### ت‑٥ · `RASID_PAUSE_PROCESSES=cs2.exe` لا يعمل أبداً
`watch.ts:112` يضيف `.exe` بنفسه، فيبحث عن `"cs2.exe.exe"`.

---

## المراجعة الثانية: زوايا لم يُكلَّف بها وكيل (٢٠٢٦‑٠٩‑٠٢)

انقطعت الجلسة مرّتين، فأُعيدت القراءة على الجبهات التي لم تدخل في تكليف أحد:
هويّة السجلّ، وسلامة البيانات، وعامل الخدمة، وأسرار المستودع.

**ما ثبت أنه سليم، ويُسجَّل حتى لا يُعاد فحصه:**
`.env` و`.profile.local` مستثنيان فعلاً ‎(`git check-ignore` أكّدهما)، ولم يُرفعا
في أي التزام سابق. لا اشتراك دفع ولا نقطة نهاية في ملفّ متتبَّع. مفتاح VAPID
الخاص يُقرأ من البيئة ولا يُكتب في المستودع. كل حقل مقروء من الشبكة يمرّ على
`esc()` قبل إدخاله في الصفحة، فلا حقن HTML من عنوان إعلان. الأصول المجزّأة
تُخدَم من الشبكة أولاً عند التنقّل، فلا نسخة قديمة عالقة عند صديق.

### ق‑٣٨ · مفتاح السجلّ هو الرابط لا الإعلان: برنامج واحد يصير خمس بطاقات متناقضة

مصرف الراجحي يحمل **٥ سجلات** لبرنامج تعاوني واحد، لأن البرنامج منشور على
خمسة روابط: صفحة عربية، وصفحة إنجليزية، وصفحة وظيفة، والجذر، والصفحة الرئيسية.
كل رابط صار سجلاً مستقلاً، وكل سجلّ صُنِّف وحده:

| الرابط | الدرجة |
|---|---|
| `careers.alrajhibank.com.sa/ar/…/coop-training-…` | **٩٥** |
| `careers.alrajhibank.com.sa/ar/page/coop-program/` | ١٠ |
| `careers.alrajhibank.com.sa/en/page/coop-program/` | ١٠ |
| `careers.alrajhibank.com.sa/` | ١٠ · مغلق منذ ٢٠٢٣ |
| `www.alrajhibank.com.sa/` | بلا درجة |

الصفحتان العربية والإنجليزية للبرنامج **نفسه** أخذتا ١٠ و٩٥.

المسح الكامل: **١٣ جهة من ٣١** تحمل أكثر من سجلّ مصنَّف واحد، ومنها اثنتان
تتناقض سجلاتهما بأربعين نقطة فأكثر (الراجحي ٨٥، أرامكو ٥٠).

ثلاثة أضرار، لا واحد:
1. القائمة تُري خمس بطاقات لفرصة واحدة، فتبدو التغطية أكبر مما هي.
2. الدرجة المعروضة تعتمد على أي بطاقة وقعت عليها العين، وليست حكماً واحداً.
3. **الإشعارات تتضاعف**: إعلان واحد يفتح على خمسة روابط يوقظ الجوّال خمس مرّات،
   وهو أسرع طريق إلى أن يُسكِت الإشعارات، فيسقط معيار الست ساعات كلّه.

الإصلاح **ليس دمج السجلات**، الدمج ممنوع بلا إذن. الإصلاح أن يُشتقّ مفتاح
الإعلان من الجهة والعنوان والتاريخ، وتُعرض السجلات المتّحدة المفتاح بطاقةً
واحدة تحمل روابطها كلّها، مع بقاء كل سجلّ على حاله في الملفّ.

### ث‑٣٦ · ٩٤ بطاقة من ١٤٧ بلا عنوان

`titleAr` في ٩٤ سجلاً هو حرفياً «لم يُصنَّف بعد»، موزّعة على **٨٦ جهة**.
هذه نتيجة إعادة إدراج البطاقات التي كانت تحمل نصّ خطأ إنجليزياً، وهي حالة
مؤقّتة تُفترض أن تزول بعد جولة تصنيف كاملة. تُسجَّل هنا لسبب واحد: **التحقّق
النهائي يجب أن يقيس هذا الرقم بعد آخر جولة**، فإن بقي على حاله فالتصنيف لا
يعمل، والقائمة الرئيسية التي سيراها صديق ثلثاها بطاقات بلا اسم.

## تصحيحات على التدقيق نفسه

سُجّلت أعطاب لم تثبت عند الفحص المباشر. تُذكر لأن سجلّاً يُنقّح نفسه أوثق من
سجلّ لا يُخطئ أبداً.

### ق‑٣٥ ليس عطباً · «لا شيء يسحب `nextCheckAt` إلى الوراء»

يسحبه. `reconcile` ‎(`schedule.ts:277‑282`)‎ يعيد حساب الموعد عند تغيّر الطبقة،
فمصدر منخفض الأولوية قُرئ قبل عشر دقائق وموعده بعد ٣٥٠ دقيقة، حين تُفتح نافذة
عند جهته ويصير `hot`، صار موعده **بعد خمس دقائق**. مُنفَّذ:

```
قبل: بعد ٣٥٠ دقيقة، الطبقة low
بعد: بعد ٥ دقائق،  الطبقة hot   ← تقدّم ٣٤٥ دقيقة
```

و`tierOf` ‎(`schedule.ts:119‑126`)‎ يرقّي المصدر فعلاً حين يحمل إعلاناً مفتوحاً
أو يغلق قريباً. لا تغيير مطلوب، ولم يُغيَّر شيء.

---

## ق‑٥٢ · النموذج نفسه هو السبب الجذري، واختياره قيس على الشيء الخطأ

ظهر أثناء الإصلاح، بعد أن أُضيف حارس ق‑٤٥. الحارس هو ما كشفه.

`llama3.1:8b` **لا يكتب عربية سليمة**. شُغِّل على حالات المرجع نفسها:

| النموذج | مقبول | ما كتبه في العنوان |
|---|---|---|
| `llama3.1:8b` | **٠ من ٣** | «الياعلان الثدرب الإعاني» · «الحداب الاشية الإليكي» · مدينة «الثيب» |
| `qwen3:8b` | **١٠ من ١٠** | «برنامج التدريب التعاوني بسدايا» · «تدريب تعاوني، تحليل البيانات» |

سلاسل llama ليست كلمات. وهي **نفس شكل** السجلات المختلَقة في البيانات الحيّة:
٩٠ للأكاديمية الوطنية للأمن السيبراني على «البراغات الالميدية»، وصفر لبنك على
عنوان لا يُقرأ. فليست ق‑١٠ حادثة، بل هي ما يفعله هذا النموذج في كل صفحة.

**ولماذا اختير؟** الجدول الذي اختاره ‎(`model.ts:50‑54`)‎ يقيس **الفرز** وحده:
سؤال بكلمة واحدة، جوابه نعم أو لا، ولا يُكتب فيه حرف عربي. llama يسقط ضجيجاً
أكثر هناك فعلاً. أما النصف الغالي، وهو نسخ سبعة عشر حقلاً **من الصفحة حرفياً**،
فلم يُقَس إطلاقاً. اختير النموذج باختبار لا يرى العمل الذي سيقضي وقته كلّه فيه.

و`qwen3:8b` أسرع أيضاً على العمل الحقيقي: ٣٣ ثانية للصفحة مقابل ٦٨ إلى ١٠٥.

**أُصلح:** صار `qwen3:8b` هو الافتراضي في `model.ts` وفي مشغّلَي المراقب.

---

## ما أضافه تدقيق شرعيّة المصادر (٢٠٢٦‑٠٩‑٠٢)

الجبهة التي سمّاها صاحب المشروع بنفسه: **هل هذه المصادر رسمية أصلاً.**
الجواب: النطاقات رسمية في معظمها، و**الصفحات ليست هي الصفحات الصحيحة**.

| القياس | العدد |
|---|---|
| مصادر يمكن أن تحمل إعلان تعاوني | **١٦٠ من ٤٦٠ · ٣٥٪** |
| صفحات مركز إعلامي وأخبار وفعاليات | ١٥١ |
| صفحات رئيسية و«عن الجهة» | ١٠٧ |
| **جهات كل مصادرها عاجزة** | **٤٩ من ١١٥ · ٤٣٪** |
| جهات بلا أي مصدر مؤكَّد أنه تعاوني | **٩١ من ١١٥** |
| جهات مراقَبة برابط واحد | ٢٨ |

**التطبيق يراقب ١١٥ جهة، ويرى صفحة تعاوني حقيقية عند ٢٤ منها.**

### ق‑٤٦ · ٤٩ جهة تفشل بصمت لأن كل صفحاتها من نوع لا يحمل إعلاناً

فئة S: `ncac` و`pif` و`site`.
فئة A: `bankaljazira` و`bsf` و`citc` و`deloitte_sa` و`gac` و`ncai` و`ncdc` و`ndf`
و`sar` و`sidf` و`simah` و`taqnia`. وفئة B ستّ، وفئة C ثمان وعشرون.

أسوأ الأنماط: `ndf` سبعة مصادر، واحد رئيسية وستّة مركز إعلامي فيها النشرات
والتقارير السنوية والهوية البصرية، ولا صفحة توظيف واحدة. و`nazaha` أربعة من
سبعة **روابط ثابتة لأخبار بعينها**، وخبر واحد لا يتغيّر أبداً.
و`gaca` صفحته «الإعلانية» عنوانها المخزَّن حرفياً `| 404`، و`etec` عنوان
رئيسيّته «حدث خطأ»، و`rcjy` مصدراه «حصل خطأ عند معالجة القالب».

وحقل `type` غير موثوق **في الاتجاهين**: بوابة `moj` التعاونية مصنّفة
`site_root`، وأرشيف نشرات `ndf` مصنّف `announcement_page`.

### ق‑٤٧ · أهمّ جهة سيبرانية في القائمة تُراقَب على موقع ميت وصفحة جهة أخرى

`ncac`، فئة S. `ncac.edu.sa` **مات** ‎(`ECONNREFUSED` اليوم)‎. الأكاديمية
انتقلت إلى `haseen.gov.sa/academy/ar/` ‎(تحويل ٣٠١ مؤكَّد)‎. والبديل المخزَّن
`nca.gov.sa/ar/education-program/` صفحة سياسة تعليم عالٍ عامة، أي صفحة خاطئة
على نطاق صحيح.

الصحيح: `https://haseen.gov.sa/academy/ar/` و`https://nca.gov.sa/ar/nca-academy/`.

وهذه هي الجهة نفسها التي أنتجت العنوان المهلوس «البراغات الالميدية» بدرجة ٩٠
في ق‑١٠. السبب الآن مفهوم: لا يوجد ما يُقرأ أصلاً.

### ق‑٤٨ · ستّ جهات تُراقَب على هدف خاطئ، لا على صفحة قديمة

| الجهة | ما يُراقَب | الواقع |
|---|---|---|
| `redf` | `housing.gov.sa/ar/related-links/140` | **جهة أخرى**، ونطاق مندمج غير قابل للوصول |
| `ssc` | `ssa.gov.sa` | عنوانه «بلا عنوان» و٤٢٠ حرفاً كلّها لافتة تحقّق gov.sa |
| `tadawul` | ستّة على `saudiexchange.sa` | صفحة التوظيف ترجع **٤٠٣ اليوم**، والخمس الباقية تقاويم إفصاح |
| `taif_amanah` | `taifcity.gov.sa/#/ar` | ما بعد `#` لا يُرسل إلى الخادم أصلاً |
| `adeem` | `adeemcapital.com` | يقدّم نفسه «أصيلة المالية · Just another WordPress site» |
| `alinma` | ٢٦ مصدراً | **٢٠ منها أخبار ٢٠٠٩‑٢٠١٤** مكرّرة عربي وإنجليزي، ولا صفحة تعاوني |

### ق‑٤٩ · صفحات التعاوني الحقيقية موجودة ومفتوحة وغائبة عن الملفّ

| الجهة | الصفحة الصحيحة الغائبة |
|---|---|
| `sidf` (A) | **`sidf.gov.sa/Coop` مفتوحة الآن** برابط تقديم حيّ |
| `sabic` (S) | `jobs.sabic.com` ‎(إعلان تعاوني بالجبيل قائم)‎ |
| `pif` (S) | `careers.pif.gov.sa` و`pif.gov.sa/ar/careers/graduate-development-program/` |
| `nca` (S) | نموذج حصين: `haseen.gov.sa/form?id=ec7b7035-…` وهو **القناة الفعلية** |
| `citc` (A) | `new.cst.gov.sa/ar/business/about/career` |
| `tadawul` (A) | `tadawulgroup.sa/wps/portal/tadawulgroup/joinus` |
| `sfda` (A) | صفحة التعاوني مدرَجة لكنها **لم تُفتح ولا مرّة** |
| `swcc` · `mawhiba` · `sar` | `swcc.gov.sa/ar/Career` · `former.mawhiba.org/…/Internship.aspx` · بريد `Training@sar.com.sa` |

### ق‑٥٠ · «أهمّ مصدر في التطبيق كلّه» لم يُفتح ولا مرّة

`data/aggregators.json` يصف نظام التدريب الميداني بجامعة الطائف
‎(`webapps.tu.edu.sa/TuITTrainSys/`)‎ بأنه **«أهم مصدر في التطبيق كله»**،
و`verifiedAtISO` فيه `null`. وكذلك `hrdf_coop`، و`mygov_coop` رابطه `null` أصلاً.

### ق‑٥١ · جهات سيبرانية تشغّل تعاوني وغائبة تماماً عن القائمة

| الجهة | التعاوني |
|---|---|
| **سرار من stc** | **تعاوني أمن سيبراني منصوص عليه.** أكبر غياب مفرد في القائمة كلّها |
| **تحاكم** | تعاوني يستهدف الأمن السيبراني وعلوم الحاسب والذكاء الاصطناعي |
| **KACST** | بوابة تعاوني مخصّصة: `coop.kacst.edu.sa` |
| **SAMI** · **المركز الوطني للمعلومات** · **زين** · **السعودية للكهرباء** · **البنك السعودي الأول** · **البريد السعودي** | كلّها تشغّل تعاوني، ولا واحدة منها في الملفّ |

وإلى جانبها: `careers.kafd.sa` و`careers.flyadeal.com` و«السعودية»
و`ncvc.gov.sa`، وكلّها تعلن تعاوني علناً.

### ث‑٤٦ · جهات لا تشغّل تعاوني في السعودية تُحسب تغطية

`unilever_sa` ستّة مصادر كلّها `unilever.com` عالمية وصفحات مستثمرين، ولا تعاوني
سعودي. `deloitte_sa` مصدر واحد هو الرئيسية الإقليمية. `pwc_sa` مصادره تشمل
**برنامج توطين إماراتياً** وآخر عُمانياً. `mckinsey_sa` سبع صفحات عالمية منها
لعبة تقييم. و`etec` و`moe` و`tvtc` **منظِّمو التدريب لا مضيفوه**، و`tvtc` هي
الجهة التي **تُرسل** المتدرّبين.

### ث‑٤٧ · ملاحظات التحقّق قديمة: عشر جهات من عشرين تخالف ما هو مخزَّن

`dga` صفحته ما زالت تقول «سيتم إعادة فتح باب التقديم في شهر مايو **٢٠٢٥**»،
أي بائتة أكثر من سنة. و`gastat` صار «عذراً، لقد تم إغلاق باب التسجيل».
و`sdaia` و`ncai` يردّان الآن `The requested URL was rejected`، أي **جدار حماية
يمنع الآلات على أهمّ هدف من فئة S**. و`aramco` ينتهي وقته من هذا العنوان.

### ث‑٤٨ · منصّة طرف ثالث واحدة، وهي زائدة ولم تُفتح

`dgacareer.talentera.com` وحدها، و`verifiedAtISO` فيها `null`، ونسختها على نطاق
الجهة `career.dga.gov.sa` **تُقرأ فعلاً**. باقي ما يبدو خارجياً هو نطاق فرعي
للجهة نفسها يشغّل نظام توظيف مستأجَراً، وهو المكان الصحيح.
لكن الفجوة المقابلة قائمة: `redf` و`site` يعلنان التعاوني عبر **لينكدإن**،
ولا قناة من هذا النوع في التطبيق إطلاقاً.

## ما أضافه تدقيق المصنّف والملاءمة والتواريخ (٢٠٢٦‑٠٩‑٠٢)

كل سطر أدناه نُفِّذ على الوحدات الحقيقية، لا استُنتج.

### ق‑٣٩ · لا مسار «لا أعرف» في الملاءمة: صفحة لم تُقرأ تصير حكماً واثقاً بدرجة ١٠
`relevance.ts:180‑194`. `relevanceOf` تعيد رقماً في كل مسار. القيمة الفارغة
الوحيدة في نصف الحكم كلّه تأتي من تعذّر قراءة **ملفّه الشخصي**، لا من تعذّر قراءة
**الصفحة**. فسجلّ بـ`majors: []` وعنوان مشوّه، وهو شكل الثلاثين سجلاً المأخوذة من
لافتة gov.sa بالضبط، يخرج هكذا:

> درجة ١٠ · «لا يذكر الإعلان تخصّصاً قريباً من تخصّصك»

هذه جملة **حكم على الإعلان**، ولم يُحكَم على شيء. هذا هو تحويل «تعذّر الحكم» إلى
«غير مناسب»، وهو **الناتج الافتراضي لكل فشل استخراج**.

### ق‑٤٠ · صياغات الأمن السيبراني السعودية الشائعة تأخذ ١٠، و«إدخال البيانات» يأخذ ٧٠
`relevance.ts:46‑47, 79‑80, 98‑105`. مُنفَّذ:

| صياغة إعلان حقيقية | الحقل | الدرجة |
|---|---|---|
| أمن الأنظمة الصناعية (أرامكو، السعودية للكهرباء، معادن) | لا شيء | **١٠** |
| أمن التحكم الصناعي وأنظمة سكادا | لا شيء | **١٠** |
| فريق الاستجابة للحوادث | لا شيء | **١٠** |
| حوكمة المخاطر والامتثال | لا شيء | **١٠** |
| المديرية العامة للدفاع المدني | لا شيء | **١٠** |
| جامعة الملك فهد للبترول والمعادن | لا شيء | **١٠** |
| الأدلة الرقمية الجنائية | عام | ٣٥ |
| **متدرّب إدخال البيانات** | تقنية | **٧٠** |
| **المحاسبة** | عام | **٣٥** |
| `Cooperative Training: apply for it now` | تقنية | ٦٥ |

سببان. الأول: قائمة السيبراني تفتقد «أمن الشبكات» و«أمن الأنظمة» و«حماية
البيانات» و«الاستجابة للحوادث» و«الأدلة الرقمية» و«الأمن الرقمي» و«حوكمة».
الثاني: قائمة غير الحاسوبية تُطابق **أجزاء الكلمات**: «مدني» داخل «المدنية»،
و«صناعية» داخل «الصناعية الرابعة»، و«بترول» داخل «للبترول والمعادن»، ثم
`relevance.ts:100` يقطع الطريق إلى تقنية وعام قبل أن يُجرَّبا.
وفي الاتجاه الآخر: «البيانات» وحدها في قائمة التقنية تلتقط أي وظيفة إدخال بيانات،
و«حاسب» **جزء من كلمة «المحاسبة»**، فكل إعلان محاسبة يأخذ ٣٥.

المرجع الحالي لا يستطيع كشف أيٍّ من هذا: حالته السيبرانية تقول «حوكمة الأمن
السيبراني» وفيها كلمة «سيبراني»، ولا حالة محاسبة ولا أنظمة صناعية فيه أصلاً.

### ق‑٤١ · نطاق تاريخين يُقرأ خطأً، والخطأ **دائماً إلى الأبكر**
`hijri.ts:154‑177` يمرّ على الأشهر بترتيب السنة لا بترتيب ورودها في النصّ.
مُنفَّذ:

```
«من 1 رجب 1448 إلى 15 شعبان 1448»  ->  خُزِّن 2026-12-10   (الصحيح 2027-01-23)
«من 2026-09-01 إلى 2026-12-30»     ->  خُزِّن 2026-09-01   -> «مغلق» اليوم
```

٤٤ يوماً مبكراً في الأولى، و**ميت عند الولادة** في الثانية. و«من … إلى …» هي
الصيغة التي تكتب بها الصفحات السعودية نافذة التقديم. ولا تحذير: `ambiguousYear`
تساوي false، فالبوابة الوحيدة التي ترفض لا تعمل.

### ق‑٤٢ · كل ثغرة في جدول الأشهر تُسلِّم التاريخ إلى النموذج، وهو ما بُني الملفّ كلّه لمنعه
`opportunity.ts:123‑134`. حين يخطئ الجدول تكون `ambiguousYear` false، فينتقل
التحكّم إلى `closesISO` الذي **حوّله النموذج بنفسه**. مُنفَّذ:

```
«غرة رمضان 1448»      -> خُزِّن 2027-01-01 من النموذج
«1 المحرم 1448»        -> خُزِّن 2030-01-01 من النموذج
«30 ذو الحجة 1448»     -> خُزِّن 2027-06-05 من النموذج
```

الثغرات المؤكَّدة: «1 المحرم»، «12 شهر صفر»، «12 من شهر ربيع الأول»،
«5 جمادى الأول»، «5 جمادى الآخر»، «12 ربيع ٢»، و«15 سبتمبر 2026» وهي **المثال
المكتوب في تعليمات النظام نفسها** ‎(`classify.ts:239`)، وأي نصّ فيه U+061C.
السبب الجذري: `hijri.ts:159` يسمح بالمسافة فقط بين الرقم والشهر، فأداة تعريف أو
كلمة «شهر» متبقّية على اليسار تقتل المطابقة. وهو **نفس عطب الحرف المتبقّي** الذي
يقول التعليق في السطر ٩٠ إنه أُصلح على اليمين.
كما أن `hijri.ts:153` يوثّق «غرة رمضان 1448» كصيغة مدعومة، وهي تعيد لا شيء.

### ق‑٤٣ · `idFor` بلا رابط: صفحتان تنهاران في سجلّ واحد، والباقي يُحذف
`opportunity.ts:20‑21` يبني المعرّف من الجهة والعنوان وتاريخ أول ظهور، **بلا
`sourceUrl`**، بينما بقيّة المسار تحذف بالرابط ‎(`collect.ts:702, 795, 864`)‎
وتخزّن في خريطة مفتاحها المعرّف ‎(`collect.ts:502`)‎ و`now` ثابتة للجولة كلّها.
مُنفَّذ: أربعة سجلات، **نجا اثنان**.

وأسوأ حالاته `asManualReview` ‎(`opportunity.ts:245`)‎ لأن عنوانه **ثابت**:
«لم يُصنَّف بعد». وانقطاع أولاما كلّه أو لا شيء، فصفحات جهة واحدة تفشل معاً في
الجولة نفسها، وتنهار في بطاقة واحدة، ولا يصل الباقي إلى طابور المراجعة أبداً.

هذا **النقيض المتناظر لـ‏ق‑٣٨**: هناك برنامج واحد يصير خمسة سجلات، وهنا خمس صفحات
تصير سجلاً واحداً. العلّة واحدة: هويّة السجلّ غير معرَّفة تعريفاً صحيحاً.

### ق‑٤٤ · حارس قِدَم المرجع يعدّ إجابات قديمة، فيكتب بصمة جديدة فوق أجوبة سابقة
`build-benchmark.ts:48, 88‑89`. الخريطة تُبذَر بالأجوبة القديمة ثم يُعدّ المدموج،
فحالة **فشلت هذه الجولة** تُحسب مُجابة. مُحاكاة بفشل العشرين كلّها:

```
answered: 20 من 20 · wholeSetRebuilt: true
سيكتب بصمة اليوم الجديدة وتاريخ اليوم واسم llama3.1:8b فوق أجوبة قديمة
حارس `answered === 0` لا يعمل
```

وهذا بالضبط ما يقول السطر ٧٩ إنه «يجب ألّا يستطيع الكذب على نفسه».
والأسوأ أنه واقع الآن في المستودع: `data/benchmark.json` يحمل
`model: "claude-sonnet-5"` وبصمة مختلفة عن الحيّة، ‏٢١ جواباً لعشرين حالة.
**المرجع المُلتزَم لم يختبر `llama3.1:8b` ولا مرّة.**

### ق‑٤٥ · لا حارس إطلاقاً على أن مخرَج النموذج كلام حقيقي، والحلّ مقيس وجاهز
تُوسِّع ق‑١٠. البوابتان الوحيدتان على أي نصّ من النموذج هما: عنوان غير فارغ
‎(`classify.ts:506`)‎، ومحارف رابط التقديم ‎(`opportunity.ts:102`)‎. مخطّط Ajv
صارم بنيوياً ومفتوح دلالياً تماماً. قُبِلت كلّها:

```
"البراغات الالميدية" · "Cooperative Training Program" · majors ["undefined","N/A"]
cities ["أي مدينة"] · statesZeroCoursesRule=true بلا اقتباس · closesISO "غداً"
```

وفي بياناتك الحيّة الآن:

| الجهة | الدرجة | ما خُزِّن |
|---|---|---|
| saib | **٠** | «بريدييم التاحيي لالكحريجين» ومدينة «الريدية» |
| saib | ١٠ | «بلاغة الثدرب الإشيالي الإصثياري» |
| hrsd | ١٠ | «الثابيق الدابة الدابة» |
| zatca | ١٠ | مدينة «الحيءة» |
| monshaat | ٩٠ | تخصّصات بالإنجليزية، خلافاً للتعليمات |
| cma | ٧٠ | اقتباس شرط من كلمتين: «انتظام كلي» |

لاحظ `saib` بدرجة **صفر**: عنوان مهلوس قُرئ «تطوير خريجين»، فأنتج الرقم الوحيد
في النظام الذي يعني «لا يستطيع التقديم».

**الحلّ المقيس:** التعليمات تأمر النموذج بأن **ينسخ** العنوان والتخصّصات والمدن
والاقتباس والتواريخ حرفياً، والمقتطف المعروض عليه معروف بدقّة
‎(`classify.ts:462, 472`)‎. فكل ما يدّعي نسخه **يجب أن يكون جزءاً من المقتطف**.
فحص `String.includes` واحد لكل حقل، بلا نموذج وبلا شبكة، بعد تطبيع التشكيل
والتطويل و(أ إ آ) و(ى) و(ة). قياسه:

- **٢٠ من ٢٠ حالة مرجعية تمرّ نظيفة**، صفر إنذار كاذب.
- يرفض **كل** سجلّ مهلوس معروف في بياناتك، ومنها الـ٩٠ لمنشآت.

ويُقرَن بإصلاح ق‑٣٩: تعيد الملاءمة قيمة فارغة، لا ١٠، حين لا تذكر الصفحة تخصّصاً.

### ث‑٣٨ · `statusOf` يحكم على تاريخ النموذج، والسجلّ يخزّن التاريخ المحلول
`opportunity.ts:139` مقابل `:164`. كائن واحد وتاريخان. لا يظهر في الإنتاج لأن
`collect.ts:884` يعيد الحساب، لكن `test-benchmark.ts:112` **يتحقّق من قيمة لا
يحتفظ بها المسار أبداً**.

### ث‑٣٩ · `readsAsNo` يبتلع كل جواب عربي يبدأ بحرفَي لام وألف
`model.ts:202`. نُقلت `\b` إلى الفرع الإنجليزي بحقّ، ولم يُوضع للعربي حدّ أصلاً.
كلّها تعيد «لا» فتُسقط الصفحة:

```
«لا شك أنها إعلان تدريب تعاوني»  ·  «لاحظ أن الصفحة تحتوي إعلان تدريب»
«لا أستطيع الجزم، وربما نعم»      ·  «لابد من مراجعة الصفحة»
```

الأخيرة تعكس أمر التعليمات نفسه «إن ترددت فقل نعم». غير قاتل اليوم فقط لأن
`json: true` يُجبر المخرَج على البدء بقوس، ويصير قاتلاً لحظة سقوط ذلك.
العلاج: `/^\s*(?:لا(?![\p{L}])|no\b|false)/iu`.

### ث‑٤٠ · `absoluteApplyUrl` يصنع روابط تقديم من نفاية النموذج
`opportunity.ts:102` يرفض المحارف غير اللاتينية فقط، أي العربية وحدها:

```
"N/A" -> https://x.gov.sa/careers/N/A      ·  "TBD" -> …/TBD  ·  "-" -> …/-
"https://x.gov.sa/التدريب" -> مرفوض        <- رابط سليم يُرمى
```

والتعليق فوقه يقول إن «رابط تقديم مكسور على موعد أسوأ من لا رابط».

### ث‑٤١ · `MAX_EXCERPT_CHARS = 6000` قصّ أعمى من الرأس
`classify.ts:462`، بلا نافذة حول الكلمة المفتاحية خلافاً لـ`triageExcerpt`.
والمواعيد تعيش في أسفل الصفحات الطويلة، فتُخزَّن فارغة.

### ث‑٤٢ · `readProfile` لا يفهم صيغة المتكلّم
`relevance.ts:127` يطابق «لم يتخرّج» ولا يطابق «لم أتخرج»، وهي الصيغة الطبيعية
لمن يكتب عن نفسه. ملفّك الحالي يصيب الفرع الصحيح، والمرجع يستخدم الإنجليزية،
فلن تُظهره أي بوابة.

### ث‑٤٣ · `TRIAGE_PROMPT` نصّ ميت
`classify.ts:369‑377` بلا أي مرجع في المستودع. التعليمة الحيّة هي `TRIAGE_SYSTEM`
في `model.ts:179`. الميتة تطلب جواب كلمة واحدة، وهو الشكل الذي يخطئ فيه ث‑٣٩.

### ث‑٤٤ · `hijri.ts:151` ينزع علامتَي اتجاه فقط
U+200F و U+200E، ويترك U+061C و U+2066‑2069، وهي شائعة في صفحات gov.sa، فتكسر
تجاور الرقم والشهر وتغذّي ق‑٤٢.

### ث‑٤٥ · `12/03/2026` يُقرأ باليوم أولاً بلا إشارة التباس
`hijri.ts:187`. صحيح بالعرف السعودي، وخاطئ بصمت حتى أحد عشر شهراً على صفحة
مكتوبة بالعرف الأمريكي.

### ت‑٦ · `build-benchmark.ts:111` يطبع «خُزِّن N جواباً» في الفرع الذي لا يكتب شيئاً
### ت‑٧ · `relevance.ts:127` بديل ثانٍ تغطّيه الشدّة الاختيارية في الأول
### ت‑٨ · مكافأة المدينة ‎+٥‎ تُمنح ولو كان الإعلان غير ذي صلة، فيصير ١٥ لا ١٠
### ت‑٩ · `data/benchmark.json` فيه جواب يتيم `it_5` بلا حالة تقابله

### ث‑٣٧ · `provenanceTag` يُدخل قيمة غير مفحوصة في سمة `class`

`main.ts:164` يبني `class="prov prov-${p}"` بلا `esc`، و`p` يأتي من ملفّ بيانات
يكتبه النموذج. النوع يمنعه وقت الترجمة ولا يمنعه وقت التشغيل. الضرر محدود
(كسر تنسيق، لا تنفيذ نصّ برمجي) لأنه داخل اقتباسين، لكنه الحقل الوحيد المتبقّي
الذي يدخل الصفحة بلا تهريب.
